import { Router } from 'express';
import { ColumnController } from './column.controller.js';
import { ColumnService } from './column.service.js';
import { prisma } from '../../db/prisma.js';
import { ColumnRepo } from './column.repo.js';
import { authMiddleware } from '../../common/middlewares/auth.middleware.js';
import { requireWorkspaceRole } from '../../common/middlewares/requireWorkspaceRole.middleware.js';
import { validateBody, validateParams, validateQuery } from '../../common/middlewares/validateRequest.middleware.js';
import { createBodySchema, listColumnQuerySchema, reOrderBodySchema, updateBodySchema } from './column.schemas.js';
import { ActivityService } from '../activity/activity.service.js';
import { ActivityRepo } from '../activity/activity.repo.js';
import { emptyBodySchema, paginationQuerySchema, workspaceParamsSchema } from '../../common/schemas/common.schemas.js';
const columnController = new ColumnController(new ColumnService(prisma, new ColumnRepo(prisma), new ActivityService(new ActivityRepo())));
const router = Router();
router.post('/:workspaceId/:projectId', authMiddleware, requireWorkspaceRole('admin'), validateParams(workspaceParamsSchema), validateBody(createBodySchema), columnController.create);
router.get('/:workspaceId/:projectId', authMiddleware, requireWorkspaceRole(), validateParams(workspaceParamsSchema), validateBody(emptyBodySchema), validateQuery(listColumnQuerySchema), columnController.listByProject);
router.get('/:workspaceId/:projectId/:columnId', authMiddleware, requireWorkspaceRole(), validateParams(workspaceParamsSchema), validateBody(emptyBodySchema), columnController.get);
router.patch('/:workspaceId/:projectId/re_order', authMiddleware, requireWorkspaceRole('admin'), validateParams(workspaceParamsSchema), validateBody(reOrderBodySchema), validateQuery(paginationQuerySchema), columnController.reOrder);
router.patch('/:workspaceId/:projectId/:columnId', authMiddleware, requireWorkspaceRole('admin'), validateParams(workspaceParamsSchema), validateBody(updateBodySchema), columnController.update);
router.delete('/:workspaceId/:projectId/:columnId', authMiddleware, requireWorkspaceRole('admin'), validateParams(workspaceParamsSchema), validateBody(emptyBodySchema), columnController.remove);
export default router;
